class Line_type:
    def __init__((bool array, int) array y, typenum)
    #  Takes in an array whose elements are (word type, number of syllables) and outputs
    self.typenum = typenum
    self.typearray = list.map (fun (a, b) -> a) y
    self.syllablearray = list.map (fun(a, b) -> b) y
    self.occurrences #counts the number of occurrences of this line_type
    self.update(haiku) #increases self_occurrences based on data from haiku
    


